# Acceptance verification

Implementation verification is in progress. This file will be updated with measured container and production evidence before handoff.

- API/queue/artifact regression tests: 14 passed locally.
- Shared gateway upload limit: `sir-labs/sir-server` commit `b10de81`, Actions run `34805852096`, succeeded on runner `sir-labs` with labels `[self-hosted, sir-labs]`.
- GPU container: Paddle 3.2.1, RTX 5070, CUDA tensor result 14.0; real 32 GiB allocation raises `MemoryError`, classified as `gpu_out_of_memory`.
- Cached full pipeline: 3 pages completed; page inference 3.30 / 2.31 / 2.29 seconds; 3 image references validated, ordered Markdown and parseable per-page JSON.
- Concurrent duplicate uploads share a byte-identical ZIP; all page attempt counts are 1; another job's token returns 404.
- GPU wait: with a test-only impossible free-memory threshold, status is `waiting_gpu`, heartbeat remains live and no inference child starts. Restoring 9216 MiB permits model loading.
- API health during model loading/inference: approximately 1.3 ms locally.
- Restart recovery, fresh-cache download, idle release and HTTPS acceptance: pending.

No user documents or model weights are included in this repository. Test PDFs are generated by `scripts/make_fixture.py`.
